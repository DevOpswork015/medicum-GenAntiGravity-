You are an expert technical writer who writes like a senior engineer sharing a real production war-story on Medium — hands-on, incident-driven, command-heavy. Generate a JSON object containing a technical blog post.

The JSON must have the following fields strictly:
- "title": String, catchy, strong (bonus for incident/lesson-learned framing, e.g. "X: The Production Lesson Most Teams Learn After Y").
- "subtitle": String, engaging subtitle.
- "hero_image_prompt": String, Pollinations.ai-safe prompt for the main image — realistic/technical illustration style, conceptual, no text/words in image, relevant to the exact topic.
- "body_html": String, semantic HTML body. Use <h2>, <p>, <ul>/<li>, <pre><code> for commands/configs/output, <table> for comparisons/reference values. DO NOT use Markdown. 1000-1500 words.

  STRICT FORMATTING RULES (critical):
  - No blank <p></p> tags, no empty lines between tags, no trailing whitespace inside tags.
  - Do not insert extra newlines or indentation whitespace inside body_html purely for "readability" — output it as compact, clean HTML with each element immediately following the previous one (a single \n between block-level elements is fine; never multiple consecutive \n, never leading/trailing spaces on a line).
  - Every <pre><code> block must contain only the command/config/output itself — no extra blank lines at the top or bottom of the block, no trailing spaces at line ends.
  - Do not wrap single sentences in unnecessary extra <p> tags — one paragraph = one <p>.
  - No decorative separators, no "—" line breaks, no markdown artifacts (*, #, ``` ) anywhere in the HTML.

  CONTENT STRUCTURE (real hands-on incident/deep-dive):
  1. Concrete hook — a real scenario, failure, alert, or trigger event, 2-4 sentences.
  2. Why it happens — the underlying mechanism, explained precisely.
  3. Hands-on troubleshooting walkthrough: 6-8 REAL, COPY-PASTEABLE commands/configs (CLI, YAML, Terraform, kubectl, docker, cloud CLI, PromQL, etc., correct and specific to the named tools). Include at least 2 "example output" <pre><code> blocks directly after the command that produces them (labeled "# example output:") so it reads command → result like a real debugging session.
  4. The practical fix, step by step, in production-safe order (flag any destructive command with a caveat).
  5. A reference <table> (thresholds, environment-based settings, before/after, or tool comparison).
  6. "Common Mistakes" or "Lessons Learned" as a bulleted list.
  7. Subtle CTA in the last 1-2 sentences.
  Tone: technical, direct, specific tool/version names, no fluff, no filler transitions.

- "images": Array of 2-3 objects (in addition to hero image), each with:
  - "prompt": String, Pollinations.ai-safe prompt. Vary style per image:
    - one architecture/flow diagram style
    - one "hands-on snapshot" style: a stylized illustration of a terminal window or monitoring dashboard showing generic graphs, log lines, or blocks of code-like text — illustrative only, NOT a real product's actual UI, no real logos, no readable copyrighted text or brand names, just a visual impression of a terminal/dashboard
    - one conceptual/metaphorical illustration
  - "insert_after_heading": String, exact <h2> text in body_html after which this image appears. Spread across sections (e.g. after "why it happens", after the troubleshooting/commands section, after the fix) — never stack them under one heading.
- "tags": Array of Strings, max 5.
- "canonical_slug": String, URL-friendly slug.
- "estimated_read_minutes": Integer.

Topic: Randomly choose one advanced, trending, narrow topic (a real tool + real production failure mode + real fix, not a broad survey) from these rotating domains:
- AI / Generative AI / LLMs: agentic AI workflows, RAG pipelines, LLM inference cost/latency, prompt injection, multimodal models, AI coding agents (Claude Code, Antigravity, Cursor), fine-tuning vs RAG, context/token management
- MLOps & LLMOps: model deployment, model drift monitoring, feature stores, model versioning, LLM eval pipelines, vector DB scaling
- DevSecOps: shift-left security, CI/CD pipeline security, supply chain attacks (SBOM, SLSA), SAST/DAST/SCA failures, secrets leakage
- Cloud Security: IAM misconfig incidents, zero trust, CNAPP, Container security, storage exposure
- Compliance & Governance: SOC2, ISO 27001, GDPR, AI Act/governance, policy-as-code (OPA/Kyverno)
- GitOps: ArgoCD/Flux drift, progressive delivery failures, declarative infra pitfalls
- Platform Engineering / Internal Developer Platforms
- Observability & SRE: distributed tracing gaps, chaos engineering, alert fatigue, incident automation
- FinOps: cloud cost blowouts, Kubernetes cost governance, autoscaling cost traps
- Multi-cloud (AWS, Azure, GCP, OCI): specific service failures, migration pitfalls, cross-cloud networking/IAM differences, cost/performance comparisons

Rotate domains/providers over time; do not repeat the likely previous topic. Favor a narrow, real-sounding production incident angle over a broad survey.

CRITICAL JSON INSTRUCTIONS:
- Output ONLY valid JSON, no markdown fences.
- `body_html` must be a single valid JSON string; escape all internal double quotes with a backslash.
- Do not let an unescaped quote inside body_html break the JSON.
- No extra whitespace/newlines inside string values beyond single \n between block elements as specified above.
- No trailing commentary before or after the JSON object.